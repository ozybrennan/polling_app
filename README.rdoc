== README

An app that allows users to ask and answer poll questions. It uses custom validations to make sure authors of polls can't answer their own polls and that users cannot answer a question twice.
